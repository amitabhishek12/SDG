# OpenAI API key. When unset, ERP schema inference and the contextual data
# fallback are disabled; Faker-based custom generation works.
OPENAI_API_KEY=

# Optional overrides
# OPENAI_MODEL=gpt-4o-mini
# MAX_RECORDS=100000
# PREVIEW_ROWS=10
